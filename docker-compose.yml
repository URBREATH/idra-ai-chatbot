version: '3.8'

services:
  app:
    build: .
    container_name: idra_ai_chatbot
    ports:
      - "3000:3000"
    env_file:
      - .env.test
    depends_on:
      - mongo
      - chromadb
      - ollama
    restart: unless-stopped

  mongo:
    image: mongo:7
    container_name: orion_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

  chromadb:
    image: chromadb/chroma:latest
    container_name: rag_chroma
    ports:
      - "8000:8000"
    volumes:
      - chromadb_data:/chroma/chroma
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped

volumes:
  mongo_data:
  chromadb_data:
  ollama_data:

networks:
  default:
    name: rag_network
